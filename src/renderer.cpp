#include <GLFW/glfw3.h>

void render() {
    // Set the background color
    glClearColor(0.1f, 0.1f, 0.1f, 1.0f);

    // Clear the screen
    glClear(GL_COLOR_BUFFER_BIT | GL_DEPTH_BUFFER_BIT);

    // TODO: Add rendering logic for the 3D room and objects
}
